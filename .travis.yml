language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
  - "3.5"
# command to install dependencies
install: true

# command to run tests
script:
  - cd tests
  - python -m unittest discover

branches:
  only:
    - master